<nav>
        <a href="/">Home</a>
        {{#if isAdmin}}
                <a href="/admin">Admin</a>
        {{/if}}
        <a href="/shop">Shop</a>
        <a href="/shop/cart">Cart</a>
        <a href="/shop/order/history">Orders</a>
        {{#if isLoggedIn}}
                <form action="/logout" method="POST">
                        <button>Logout</button>
                </form>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
</nav>